export enum SportTypes {
  Running = '100',
}

export enum FileType {
  fit = '4',
  tcx = '3',
  gpx = '1',
  kml = '2',
  csv = '0',
}

export enum ResponseCodes {
  Success = '0000',
  LoginError = '1030',
}
